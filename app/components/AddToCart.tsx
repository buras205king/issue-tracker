'use client'
import React from 'react'

const AddToCart = () => {
  return (
   <button className="btn btn-secondary" onClick={()=>alert("add to card")}>click</button>
  )
}

export default AddToCart;